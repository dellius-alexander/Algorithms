
pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/milestone' }
        maven { url 'https://repo.spring.io/snapshot' }
        gradlePluginPortal()
    }
}

rootProject.name = 'BreadthFirstSearch'

includeBuild('../Dijkstra')

include 'org.example:Dijkstra:1.0'
